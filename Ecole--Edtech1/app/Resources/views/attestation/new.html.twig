{% extends 'Front/front.base.html.twig' %}

{% block body %}
    <h1>Attestation creation</h1>

    {{ form_start(form) }}
        {{ form_widget(form) }}
        <input type="submit" value="Create" />
    {{ form_end(form) }}

    <ul>
        <li>
            <a href="{{ path('attestation_index') }}">Confirmer</a>
        </li>
    </ul>
{% endblock %}
