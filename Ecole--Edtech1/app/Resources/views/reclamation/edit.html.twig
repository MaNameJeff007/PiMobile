{% extends 'Front/front.base.html.twig' %}

{% block body %}
<div class="container">
    <div class="row">
        <div class="col-md-8">
            <h1 class="section-title">Modifier une réclamation</h1>


    <ul>

        <li>
        {{ form_start(edit_form) }}
            <div class="form-group">

            <label>{{ form_label(edit_form.type) }}</label>
            {{ form_widget(edit_form.type, {'attr': {'class': 'form-control'}}) }}
            </div>
            <div class="form-group">
            <label>{{ form_label(edit_form.nomobjet) }}</label>
            {{ form_widget(edit_form.nomobjet, {'attr': {'class': 'form-control'}}) }}
            </div>
            <div class="form-group">
                <label>{{ form_label(edit_form.description) }}</label>
                {{ form_widget(edit_form.description, {'attr': {'class': 'form-control'}}) }}
            </div>
            <input type="submit" value="Enregistrer les modifications" class="btn btn-two" />
        {{ form_end(edit_form) }}

        </li>
        <br/>
        <li>
            <a href="{{ path('reclamation_index') }}" class="btn">Consulter vos réclamations</a>
        </li>
        <br/>
        <li>
            <a href="{{ path('supprimer_reclamation', { 'id': reclamation.id }) }}" class="btn">Supprimer la réclamation</a>
        </li>
    </ul>
        </div>
    </div>
</div>
{% endblock %}
